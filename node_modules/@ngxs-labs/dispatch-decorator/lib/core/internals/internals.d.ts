import { Observable } from 'rxjs';
export declare type Action<T = unknown> = new (payload?: T) => any;
export declare type DispatchFactory = (actionOrActions: ActionOrActions) => void;
export declare type ActionOrActions = Action | Action[];
/**
 * This can be a plain action/actions or Promisified/streamifed action/actions
 * ```typescript
 * @Dispatch() increment = () => new Increment();
 * // OR
 * @Dispatch() increment = () => Promise.resolve(new Increment());
 * ```
 */
export declare type Wrapped = ActionOrActions | Observable<ActionOrActions> | Promise<ActionOrActions>;
export interface DispatchOptions {
    cancelUncompleted?: boolean;
}
